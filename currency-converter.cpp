#include<iostream>
using namespace std;
int main(){
    double amount;
    string from, to;
    
    cout << "Enter currency from-to (e.g. 100 USD INR): ";
    cin >> amount >> from >> to;

    

    return 0;
}